/// Defines modes to determine how a navigation label is displayed.
enum NavigationLabelType {
  /// Hides all labels.
  hideAll,

  /// Only shows the selected item's label.
  onlyShowSelected,

  /// Shows all labels.
  showAll,
}
